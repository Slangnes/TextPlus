import { describe, it } from 'vitest';

/**
 * TextPlus Convert - M4 Codegen Roadmap Stubs
 *
 * DSL generation for linear transcripts is covered by real tests in
 * transcript.test.ts (including the compile round-trip acceptance test).
 * The remaining M4 generators are tracked as explicit todos.
 */

describe('TextPlus Convert - code generation (M4)', () => {
  it.todo('generates standalone HTML via @textplus/core');
  it.todo('generates Trizbort-compatible map files');
  it.todo('generates branching DSL from merged transcripts');
});
